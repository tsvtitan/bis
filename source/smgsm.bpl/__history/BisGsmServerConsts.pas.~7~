unit BisGsmServerConsts;

interface

uses BisServerModules;

const
  DefaultTypeMessage=0;

resourcestring

  SParamMode='Mode';
  SParamComPort='ComPort';
  SParamInterval='Interval';
  SParamStorages='Storages';
  SParamMaxCount='MaxCount';
  SParamTimeout='Timeout';
  SParamImei);
  SParamImsi);

  SParamBaudRate).AsInteger);
  SParamDataBits).AsInteger);
  SParamStopBits).AsInteger);
  SParamParityBits).AsInteger);
  SParamUnknownSender).AsString;
  SParamUnknownCode).AsString;
  SParamPeriod).AsInteger;
  SParamDestPort).AsInteger;
  SParamSrcPort).AsInteger;
  SParamOperatorIds).AsString);

var
  ServerModule: TBisServerModule=nil;  

implementation

end.
